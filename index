import React from "react";
import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

export default function HomePhysioLanding() {
  return (
    <div className="min-h-screen bg-white text-slate-800">

      {/* TOP TRUST BAR */}
      <div className="bg-slate-900 text-white text-sm py-3 text-center">
        Specialist Neurological & Home Physiotherapy • Same Therapist Continuity • Evidence‑Based Care
      </div>

      {/* HERO */}
      <section className="py-24 px-6 bg-gradient-to-br from-slate-50 to-white">
        <div className="max-w-7xl mx-auto grid lg:grid-cols-2 gap-16 items-center">

          <div>
            <motion.h1
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6 }}
              className="text-5xl font-bold leading-tight mb-6"
            >
              Specialist Home Physiotherapy That Restores Independence — Not Just Mobility.
            </motion.h1>

            <p className="text-xl text-slate-600 mb-8 leading-relaxed">
              For stroke, Parkinson’s, MS and long‑term mobility conditions. Structured, progressive rehabilitation delivered at home — with measurable outcomes and true clinical continuity.
            </p>

            <div className="flex flex-col sm:flex-row gap-4 mb-6">
              <Button className="rounded-2xl px-8 py-4 text-lg shadow-lg">
                Book Your Free 20‑Minute Clinical Call
              </Button>
              <Button variant="outline" className="rounded-2xl px-8 py-4 text-lg">
                View Patient Results
              </Button>
            </div>

            <p className="text-sm text-slate-500">
              No obligation. Speak directly with a specialist clinician.
            </p>
          </div>

          <div className="bg-slate-100 rounded-3xl h-[420px] flex items-center justify-center shadow-inner">
            <p className="text-slate-500">Professional In‑Home Treatment Image</p>
          </div>
        </div>
      </section>

      {/* PROBLEM SECTION */}
      <section className="py-20 px-6 bg-white">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-3xl font-semibold mb-6">
            Why Standard Physiotherapy Often Falls Short
          </h2>
          <p className="text-lg text-slate-600 leading-relaxed">
            Short appointments. Generic programmes. Long gaps between sessions. For complex neurological and long‑term mobility conditions, fragmented care leads to stalled progress and preventable decline.
          </p>
        </div>

        <div className="grid md:grid-cols-4 gap-8 max-w-6xl mx-auto mt-14">
          {[
            "Fragmented short‑term care",
            "Low exercise progression",
            "Travel & waiting list delays",
            "Lack of specialist continuity",
          ].map((item, index) => (
            <Card key={index} className="rounded-2xl shadow-md">
              <CardContent className="p-8 text-center">
                <p className="text-slate-700 font-medium">{item}</p>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* AUTHORITY / APPROACH */}
      <section className="py-24 px-6 bg-slate-50">
        <div className="max-w-6xl mx-auto grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <h2 className="text-3xl font-semibold mb-6">
              A Structured, Measurable Rehabilitation Model
            </h2>
            <ul className="space-y-4 text-lg text-slate-700">
              <li>• Home‑first specialist treatment tailored to daily function</li>
              <li>• Long‑term progressive plans with objective outcome measures</li>
              <li>• Higher‑intensity coaching between visits</li>
              <li>• Same therapist continuity and monthly performance reviews</li>
            </ul>
            <div className="mt-8">
              <Button className="rounded-2xl px-8 py-4 text-lg">
                Speak to a Specialist
              </Button>
            </div>
          </div>

          <div className="bg-white rounded-3xl p-10 shadow-xl">
            <h3 className="text-xl font-semibold mb-6">Measured Outcomes Focus On:</h3>
            <ul className="space-y-3 text-slate-600">
              <li>• Walking distance & gait quality</li>
              <li>• Balance confidence & fall reduction</li>
              <li>• Pain frequency & flare‑up control</li>
              <li>• Daily task independence</li>
            </ul>
          </div>
        </div>
      </section>

      {/* HOW IT WORKS */}
      <section className="py-24 px-6 bg-white">
        <div className="max-w-6xl mx-auto text-center mb-16">
          <h2 className="text-3xl font-semibold mb-4">Clear Pathway to Recovery</h2>
          <p className="text-lg text-slate-600">
            From first conversation to sustained independence.
          </p>
        </div>

        <div className="grid md:grid-cols-5 gap-6 max-w-6xl mx-auto">
          {[
            "Free Clinical Phone Assessment",
            "Comprehensive In‑Home Evaluation",
            "Tailored Specialist Treatment",
            "Guided Home Exercise Programme",
            "Monthly Objective Review",
          ].map((step, index) => (
            <Card key={index} className="rounded-2xl shadow-sm">
              <CardContent className="p-6 text-center">
                <div className="text-2xl font-bold mb-3 text-slate-900">
                  {index + 1}
                </div>
                <p className="text-slate-600">{step}</p>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* TESTIMONIALS */}
      <section className="py-24 px-6 bg-slate-50">
        <div className="max-w-6xl mx-auto text-center mb-16">
          <h2 className="text-3xl font-semibold mb-4">What Patients & Families Say</h2>
        </div>

        <div className="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {[1,2,3].map((item) => (
            <Card key={item} className="rounded-2xl shadow-lg">
              <CardContent className="p-8">
                <p className="text-slate-600 mb-6">
                  “We saw measurable improvement within weeks. The consistency and expertise made all the difference.”
                </p>
                <p className="font-semibold text-slate-800">Patient Family</p>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* FINAL CTA */}
      <section className="py-24 px-6 bg-slate-900 text-white text-center">
        <div className="max-w-3xl mx-auto">
          <h2 className="text-4xl font-bold mb-6">
            Ready to Start Structured, Specialist Rehabilitation?
          </h2>
          <p className="text-lg text-slate-300 mb-10">
            Book your free clinical call and find out what measurable progress could look like for you or your family member.
          </p>
          <Button className="rounded-2xl px-10 py-5 text-lg bg-white text-slate-900 hover:bg-slate-200">
            Book Your Free 20‑Minute Call
          </Button>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="bg-black text-slate-400 py-8 text-center text-sm">
        © 2026 Specialist Home Physiotherapy Service. All rights reserved.
      </footer>

    </div>
  );
}
